<%- include('../partials/header') %>

<h1>All Walks</h1>

<a href='walks/new'>New Walk</a>

<% walkDoc.forEach(function(w) { %>
    <ul class="indexwalklog">
      <li><%= user.name %></li>
      <li><%= w.caption %></li>
      <li><%= w.date %></li>
      <li><%= w.distanceTraveled %></li>
      <a href='/walks/<%= w._id %>' class="detailsbtn">DETAILS</a>
    </ul>
    <% }); %>




<%- include('../partials/footer') %>